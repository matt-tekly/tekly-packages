namespace TeklySample.App
{
    /// <summary>
    /// Summary of data necessary to configure the app at runtime
    /// Should only contain value types so that it can be duplicated to create a history.
    /// </summary>
    public class AppData
    {
        public string ActiveWorld = "home";
    }
}